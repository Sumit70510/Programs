<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>USER</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
     <div class="w-full h-full bg-zinc-900 text-white p-10">

        <h1 class="text-3xl tracking-tighter ml-3 pb-3">
         Update User
        </h1>
        <form action="/Update/<%= user._id %>" method="POST">
            <input class="px-5 py-2 bg-transparent border-2 border-zinc-800 rounded-lg outline-none" 
             placeholder="Name" value= "<%= user.name %>" type="text" name="name"> 
            <input class="px-5 py-2 bg-transparent border-2 border-zinc-800 rounded-lg outline-none" 
             placeholder="Email" value="<%= user.email %>" type="text" name="email"> 
            <input class="px-5 py-2 bg-transparent border-2 border-zinc-800 rounded-lg outline-none" 
             placeholder="Image" value="<%= user.image %>" type="text" name="image"> 
            <input class="px-5 py-2 bg-yellow-500 text-black rounded-lg ml-3" type="submit" value="Update"> 
        </form>
     </div>  
</body>
</html>