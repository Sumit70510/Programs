<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>USER</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full h-full overflow-x-auto bg-zinc-900 p-10 text-white">
         <div class="nav mb-10">
            <a class="text-blue-500" href="/">
               Back to Home 
            </a>
        </div>
        <h1 class="text-3xl tracking-tighter mb-3"> All Users </h1>
        
        <div class="flex flex-wrap gap-3">
            <% if(users.length > 0) { %>
              <%users.forEach(function(user){ %>    
            <div class="w-64 p-4 bg-zinc-800 rounded-lg">
             <div class="overflow-hidden w-full h-64 bg-zinc-700">
                <img class="w-full h-full object-cover object-top" src="<%= user.image %>">
             </div>      
             <h3 class="text-xl tracking-tighter mt-3">
               <%= user.name %>
             </h3>  
             <h5 class="text-zinc-500">
               <%= user.email %>
             </h5>
             <div class="flex mt-2 gap-4 justify-between">
                <a class="text-zinc-200" href="/edit/<%= user._id %>">Edit User</a>
                <a class="text-red-500" href="/delete/<%= user._id %>">Delete User</a>
             </div>
             </div>
              <%} )%>
            <% } else{ %>
            <h3 class="text-zinc-500">
                No Users Yet
            </h3>
            <% } %>  
        </div>
    </div>
</body>
</html>