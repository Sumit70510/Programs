#include<bits/stdc++.h>
using namespace std;
 /*#define loop(x, n) for(int x = 0; x < n;x++)
 class temp2;
 class temp
 {public:
   int something;
     temp(temp& a)
     {something = a.something;}
     temp()
     {something=100;}
     friend void frnd(temp a,temp2 b);
 } z;
 class temp2
 { public:
  int something;
  temp temp::a.temp(z);
     temp2(/* args *)
     {something=100;}
  friend void frnd(temp a,temp2 b);
 };
 void frnd(temp a,temp2 b)
 {cout<<"\nFriend Function is Working";
   cout<<a.something<<"\n"<<b.something<<endl;}
 */int main()
 {  int x=INT_MAX;
   /* temp2 B;
    cout<<B.a.something<<endl;
     
     cout<<"OK TESTED";*/
     cout<<x;
     return 0;
 }